{% extends 'backBase.html.twig' %}

{% block title %}Categorie Evenement{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <thead>
        <tr>
            <th>Nom</th>
            <th>actions</th>
        </tr>
        </thead>
        <tbody>
        {% for categorie in categorie_evenements %}
            <tr>
                <td>{{ categorie.nom }}</td>
                <td>
                    <a href="{{ path('app_categorie_evenement_edit', {'id': categorie.id}) }}">edit</a>

                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_categorie_evenement_new') }}">Create new</a>
{% endblock %}
