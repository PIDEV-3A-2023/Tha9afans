{% extends 'base.html.twig' %}

{% block title %}PaymentS{% endblock %}

{% block body %}
    <h1>PaymentS</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ payment.id }}</td>
            </tr>
            <tr>
                <th>CardNumber</th>
                <td>{{ payment.cardNumber }}</td>
            </tr>
            <tr>
                <th>CardHolder</th>
                <td>{{ payment.cardHolder }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ payment.date ? payment.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Cvv</th>
                <td>{{ payment.cvv }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_payment_s_index') }}">back to list</a>

    <a href="{{ path('app_payment_s_edit', {'id': payment.id}) }}">edit</a>

    {{ include('payment_s/_delete_form.html.twig') }}
{% endblock %}
