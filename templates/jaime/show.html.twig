{% extends 'base.html.twig' %}

{% block title %}Jaime{% endblock %}

{% block body %}
    <h1>Jaime</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ jaime.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_jaime_index') }}">back to list</a>

    <a href="{{ path('app_jaime_edit', {'id': jaime.id}) }}">edit</a>

    {{ include('jaime/_delete_form.html.twig') }}
{% endblock %}
