{% extends 'base.html.twig' %}

{% block title %}Commandeproduit index{% endblock %}

{% block body %}
    <h1>Commandeproduit index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Qgdfgdftite</th>
                <th>Produit</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for commandeproduit in commandeproduits %}
            <tr>
                <td>{{ commandeproduit.id }}</td>
                <td>{{ commandeproduit.quantite }}</td>
                <td>{{ commandeproduit.idProduit }}</td>

                <td>
                    <a href="{{ path('app_commande_produit_show', {'id': commandeproduit.id}) }}">show</a>
                    <a href="{{ path('app_commande_produit_edit', {'id': commandeproduit.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_commande_produit_new') }}">Create new</a>
{% endblock %}
