<form method="post" action="{{ path('app_commande_produit_delete', {'id': commandeproduit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commandeproduit.id) }}">
    <button class="btn">Delete</button>
</form>
