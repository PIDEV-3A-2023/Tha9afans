<form method="post" action="{{ path('app_panier_produit_delete', {'id': panierproduit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ panierproduit.id) }}">
    <button class="btn">Delete</button>
</form>
