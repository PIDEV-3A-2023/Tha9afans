{% extends 'base.html.twig' %}

{% block title %}Panierproduit index{% endblock %}

{% block body %}
    <h1>Panierproduit index</h1>

   {# <table class="table">
        <thead>
            <tr>
                <th>image</th>
                <th>nom</th>
                <th>description</th>
                <th>Quantity</th>
                <th>prix</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for panierproduit in panierproduits %}
            <tr>
                <td>

                {% if panierproduit.idProduit.image is not empty %}
                    <img src="{{ path('produitimageshow', {'id': panierproduit.idProduit}) }}">
                {% else %}
                    Aucune photo disponible
                {% endif %}
                </td>



                <td> {{ panierproduit.idProduit.nom }}</td>
               <td>{{ panierproduit.idProduit.description}}</td>
                <td>{{ panierproduit.quantity }}</td>
                <td>{{ panierproduit.idProduit.prix }}</td>

                #}{#<td>{{ panierproduit.idPanier.total }}</td>#}{#

                <td>
                    <a href="{{ path('app_panier_produit_show', {'id': panierproduit.id}) }}">show</a>
                    <a href="{{ path('app_panier_produit_edit', {'id': panierproduit.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">No records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>#}

  {#  <a href="{{ path('app_panier_produit_new') }}">Create new</a>#}



<section class="pt-5 pb-5" style="color: #0a0d13">
    <div class="container">
        <div class="row w-100">
            <div class="col-lg-12 col-md-12 col-12">
                <h3 class="display-5 mb-2 text-center">Shopping Cart</h3>
                <p class="mb-5 text-center">
                    {# <i class="text-info font-weight-bold">3</i> items in your cart</p>#}
                <table id="shoppingCart" class="table table-condensed table-responsive">
                    <thead>
                    <tr>
                        <th style="width:60%">Product</th>
                        <th style="width:12%">Price</th>
                        <th style="width:10%">Quantity</th>
                        <th style="width:16%"></th>
                    </tr>
                    </thead>
                    <tbody>
                    {% set subtotal = 0.0 %}
                    {% for panierproduit in panierproduits %}
                        <tr>
                            <td data-th="Product">
                                <div class="row">
                                    <div class="col-md-3 text-left">
                                        <img src="{{ path('produitimageshow', {'id': panierproduit.idProduit}) }}">
                                        {#
                                        <img src="https://via.placeholder.com/250x250/5fa9f8/ffffff" alt="" class="img-fluid d-none d-md-block rounded mb-2 shadow ">
                                        #}
                                    </div>
                                    <div class="col-md-9 text-left mt-sm-2">
                                        <h4> {{ panierproduit.idProduit.nom }}</h4>
                                        <p class="font-weight-light">{{ panierproduit.idProduit.description }}</p>
                                    </div>
                                </div>
                            </td>
                            <td data-th="Price">{{ panierproduit.idProduit.prix }} DT</td>




                            <td data-th="Quantity">
                                <input type="number" class="form-control form-control-lg text-center" value="1" min="1" name="quantite" id="quantite_{{ panierproduit.id }}">
                            </td>



                            <td class="actions" data-th="">
                                <div class="text-right">
                                    <button class="btn btn-white border-secondary bg-white btn-md mb-2">

                                        <i class="fas fa-sync"></i>

                                    </button>
                                    <a href="{{ path('app_panier_produit_remove', {'id': panierproduit.id}) }}" class="btn btn-white border-secondary bg-white btn-md mb-2">
                                        <i class="fas fa-trash"></i>
                                    </a>

                                    <button class="btn btn-white border-secondary bg-white btn-md mb-2">
                                        <i class="fas fa-trash"></i>

                                    </button>
                                </div>
                            </td>
                        </tr>
                        <tr>
                        </tr>
                        {% set subtotal = subtotal + panierproduit.idProduit.prix %}
                    {% endfor %}
                    </tbody>
                </table>
                <div class="float-right text-right">
                    <h4>Subtotal:</h4>
                    <h1>{{ subtotal }} DT</h1>
                </div>
            </div>
        </div>
        <div class="row mt-4 d-flex align-items-center">
            <div class="col-sm-6 order-md-2 text-right">
                {# <a href="catalog.html" class="btn btn-primary mb-4 btn-lg pl-5 pr-5">Checkout</a>#}
                <a href="card.html.twig" class="btn btn-outline-primary mb-4 btn-lg pl-5 pr-5">Checkout</a>
            </div>
            <div class="col-sm-6 mb-3 mb-m-1 order-md-1 text-md-left">
                <a href="catalog.html">
                    <i class="fas fa-arrow-left mr-2"></i> Continue Shopping</a>

            </div>
        </div>
    </div>
</section>

            {% endblock %}
